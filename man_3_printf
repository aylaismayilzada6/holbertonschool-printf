.TH _PRINTF 3 "January 2026" "1.0" "Holberton School"
.SH NAME
_printf \- produce output according to a format

.SH SYNOPSIS
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf
function produces output according to the format string pointed to by
.I format.
It writes the output to the standard output (stdout). 

The format string is composed of zero or more directives:
characters to be printed literally and format specifiers introduced by the '%' character.  

Currently supported conversion specifiers:

.TP
.B c
Print a single character.

.TP
.B s
Print a string of characters.

.TP
.B %
Print a literal percent sign.

.TP
.B d, i
Print a signed decimal integer.

.SH RETURN VALUE
Upon successful completion, _printf returns the total number of characters printed, not including the terminating null byte used internally.

If an error occurs or the format string ends with a solitary '%', _printf returns -1.

.SH ERRORS
No errno is set by _printf.

.SH EXAMPLE
.nf
_printf("Character: %c\n", 'A');
_printf("String: %s\n", "Hello, world!");
_printf("Percent: %%\n");
_printf("Number: %d\n", 42);
.fi

.SH AUTHOR
Ayla Ismayilzada, Turkan Bagirzada 

